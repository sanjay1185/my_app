<% form_for(:image, :url => user_images_path(@user),:html => { :multipart => true, :onSubmit => "$('spinner').show()" }) do |f| -%>
 <%= error_messages_for :image %>
  
  <%= hidden_field_tag "return_path", user_images_path %>
  <%= f.file_field :uploaded_data %>
  <%= submit_tag "Add this image" %>
  <%end%>

